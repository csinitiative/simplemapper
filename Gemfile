# provided by default; leaving since you need at least one source
source :gemcutter

# Required specifically by our test suite
gem "rake"
gem "shoulda"
gem "mocha"

# Required for the :simple_uuid attribute type
gem 'simple_uuid'
